"""T3: builds the model input text from InferRequest.history (see specs/04-transform.md)."""


def extract_features(history: list[dict]) -> str:
    # TODO: take the last history item and return mask_pii(clean_message(text)) (ADR-003).
    raise NotImplementedError
